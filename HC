SELECT "ACTMST"."ACNEXTAMT", "REGMST"."RGCPLNM", "REGMST"."RGCPFNM", "REGMST"."RGNNO", "REGMST"."RGCPAD1", "REGMST"."RGCPAD2", "REGMST"."RGCPCITY", "REGMST"."RGCPST", "REGMST"."RGCPZIP", "REGMST"."RGCPCTRY", "REGMST"."RGCPREGN", "EVENTS"."EMNNO", "RESERVATIONS"."RSNNO", "RESERVATIONS"."RSDTARR", "RESNIGHTS"."RNMPERNIGHTCHARGE", "RESERVATIONS"."RSDTDEP", "ROOMMATES"."RMNNO", "ACTMST"."ACNDSBAMT", "RESERVATIONS"."RSCCCTYPE", "HOTEL"."HTNTAXINC", "HOTEL"."HTCACRO", "EVENTS"."EMCNM", "HOTEL"."HTCDSC", "ROOMTYPES"."RTCACRO", "ROOMMATES"."RSNNO", "ROOMMATES"."RMDTARR", "ROOMMATES"."RMDTDEP", "ROOMMATES"."RMCLNM", "ROOMMATES"."RMCFNM", "HOTEL"."HTNNO", "RESERVATIONS"."RSCCCNO", "RESERVATIONS"."RSCCCEXP", "RESERVATIONS"."RSDTXLD", "CATEGORY"."CMNNO", "REGUSER"."RUNNO"
 FROM   (((((((("ICM"."dbo"."EVENTS" "EVENTS" INNER JOIN "ICM"."dbo"."REGMST" "REGMST" ON "EVENTS"."EMNNO"="REGMST"."EMNNO") INNER JOIN "ICM"."dbo"."ACTMST" "ACTMST" ON "REGMST"."RGNNO"="ACTMST"."RGNNO") LEFT OUTER JOIN "ICM"."dbo"."REGUSER" "REGUSER" ON "REGMST"."RGNNO"="REGUSER"."RGNNO") INNER JOIN "ICM"."dbo"."CATEGORY" "CATEGORY" ON "REGMST"."CMNNO"="CATEGORY"."CMNNO") INNER JOIN "ICM"."dbo"."RESERVATIONS" "RESERVATIONS" ON "ACTMST"."RSNNO"="RESERVATIONS"."RSNNO") INNER JOIN "ICM"."dbo"."HOTEL" "HOTEL" ON "RESERVATIONS"."HTNNO"="HOTEL"."HTNNO") LEFT OUTER JOIN "ICM"."dbo"."RESNIGHTS" "RESNIGHTS" ON "RESERVATIONS"."RSNNO"="RESNIGHTS"."RSNNO") LEFT OUTER JOIN "ICM"."dbo"."ROOMMATES" "ROOMMATES" ON "RESERVATIONS"."RSNNO"="ROOMMATES"."RSNNO") LEFT OUTER JOIN "ICM"."dbo"."ROOMTYPES" "ROOMTYPES" ON "RESERVATIONS"."RTNNO"="ROOMTYPES"."RTNNO"
 WHERE  "EVENTS"."EMNNO"=0
 ORDER BY "EVENTS"."EMNNO"


 gpt i want to use this above sql to get the records in below sp and gpt remember select will be same but order you check below in sp




CREATE PROCEDURE sp_ICM_RPT_Ht142_RDLC             
@EMNNO int                
As                
/*              
Created By : Pravendra              
Created on :04-02-2026              
Description :Get report data for Hotel - Cancellations (HT142 Report )              
*/             
BEGIN              
Set NoCount On                 
SET ANSI_WARNINGS OFF                 
         
         
SELECT Distinct RESERVATIONS.RSNNO, RESERVATIONS.RGNNO, RESERVATIONS.HTNNO, RESERVATIONS.RTNNO, RESERVATIONS.RCNNO, RESERVATIONS.RCNNO,  RESERVATIONS.HBNNO  , RESERVATIONS.RSDTARR , RESERVATIONS.RSDTDEP , RESERVATIONS.RSDTXLD , RESERVATIONS.RSCCCTYPE , RESERVATIONS.RSCCCNO, RESERVATIONS.RSCCCEXP,         
REGMST.EMNNO, REGMST.RGCNO, REGMST.CMNNO,REGMST.EMNNO, REGMST.CMNNO , REGMST.RGCPLNM , REGMST.RGCPFNM , REGMST.RGCBAD1 , REGMST.RGCBAD2 ,REGMST.RGCBCITY, REGMST.RGCPST, REGMST.RGCPZIP,REGMST.RGCPCTRY,  REGMST.RGCPREGN,         
Hotel.HTCDSC, Hotel.HTCACRO ,Hotel.HTNTAXINC,        
Events.EMCNM, Events.CUNNO, Events.MINNO, Events.CMNNO,        
ROOMTYPES.CUNNO, ROOMTYPES.RTCACRO,        
--ROOMMATES.RMNNO,      
ROOMMATES.RSNNO ,ROOMMATES.RMCLNM , ROOMMATES.RMCFNM, ROOMMATES.RMDTARR, ROOMMATES.RMDTDEP,        
ACTMST.ACNNO,ACTMST.EMNNO,ACTMST.CMNNO,ACTMST.RGNNO,ACTMST.ABNNO,ACTMST.SBNNO,ACTMST.RSNNO, ACTMST.AVNNO,ACTMST.SPNNO,ACTMST.ACNTAXAMT, ACTMST.ACNDSBAMT      
FROM RESERVATIONS         
INNER JOIN REGMST        
ON RESERVATIONS.RGNNO = REGMST.RGNNO        
INNER JOIN Hotel         
ON RESERVATIONS.HTNNO = Hotel.HTNNO        
INNER JOIN Events        
ON REGMST.EMNNO = Events.EMNNO        
INNER JOIN ROOMTYPES        
ON RESERVATIONS.RTNNO = ROOMTYPES.RTNNO        
INNER JOIN ROOMMATES        
ON ROOMMATES.RSNNO =  RESERVATIONS.RSNNO        
INNER JOIN ACTMST        
--ON RESERVATIONS.RSNNO  = ACTMST.RSNNO AND REGMST.RGNNO = ACTMST.RGNNO AND  REGMST.EMNNO = ACTMST.EMNNO AND REGMST.CMNNO = ACTMST.CMNNO        
ON RESERVATIONS.RSNNO  = ACTMST.RSNNO AND REGMST.RGNNO = ACTMST.RGNNO        
        
WHERE REGMST.EMNNO = @EMNNO AND  RESERVATIONS.RSDTXLD IS NOT NULL AND RESERVATIONS.RSDTXLD <> '' ;       
              
END 
