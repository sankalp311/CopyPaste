=IIF(Len(Trim(First(Fields!HTCAD1.Value) & "")) > 0,
    Trim(First(Fields!HTCAD1.Value) & ""),
    ""
)
& IIF(Len(Trim(First(Fields!HTCAD2.Value) & "")) > 0,
    IIF(Len(Trim(First(Fields!HTCAD1.Value) & "")) > 0, "<br/>", "") & Trim(First(Fields!HTCAD2.Value) & ""),
    ""
)
& IIF(Len(Trim(First(Fields!HTCADD.Value) & "")) > 0,
    IIF(
        Len(Trim(First(Fields!HTCAD1.Value) & "")) > 0
        OR Len(Trim(First(Fields!HTCAD2.Value) & "")) > 0,
        "<br/>",
        ""
    ) & Trim(First(Fields!HTCADD.Value) & ""),
    ""
)
& IIF(Len(Trim(First(Fields!HTCREGN.Value) & "")) > 0,
    IIF(
        Len(Trim(First(Fields!HTCAD1.Value) & "")) > 0
        OR Len(Trim(First(Fields!HTCAD2.Value) & "")) > 0
        OR Len(Trim(First(Fields!HTCADD.Value) & "")) > 0,
        "<br/>",
        ""
    ) & Trim(First(Fields!HTCREGN.Value) & ""),
    ""
)
& IIF(Len(Trim(First(Fields!HTCCNTRY.Value) & "")) > 0,
    IIF(
        Len(Trim(First(Fields!HTCAD1.Value) & "")) > 0
        OR Len(Trim(First(Fields!HTCAD2.Value) & "")) > 0
        OR Len(Trim(First(Fields!HTCADD.Value) & "")) > 0
        OR Len(Trim(First(Fields!HTCREGN.Value) & "")) > 0,
        "<br/>",
        ""
    ) & Trim(First(Fields!HTCCNTRY.Value) & ""),
    ""
)



add this value first too in above logic in rdlc expression
=First(Fields!HTCDSC.Value)
